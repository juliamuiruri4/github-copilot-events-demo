# Server port (default: 4000)
PORT=4000

# Comma-separated list of origins that are allowed to call the API.
# Example for production: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# Defaults to http://localhost:3000 when not set (local development).
ALLOWED_ORIGINS=http://localhost:3000
